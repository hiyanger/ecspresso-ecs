name: Deploy to ECS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      # OIDC認証（推奨）
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1

      - uses: aws-actions/amazon-ecr-login@v2
        id: ecr
      
      - name: Build and push
        env:
          IMAGE: ${{ steps.ecr.outputs.registry }}/ecspresso-ecr:${{ github.sha }}
        run: |
          docker build -t $IMAGE ./app
          docker tag $IMAGE ${IMAGE%:*}:latest
          docker push $IMAGE
          docker push ${IMAGE%:*}:latest

      - name: Setup ecspresso
        uses: kayac/ecspresso@v2
        with:
          version: v2.7.0
      
      - name: Deploy with ecspresso
        env:
          IMAGE_URI: ${{ steps.ecr.outputs.registry }}/ecspresso-ecr:${{ github.sha }}
        run: |
          ecspresso deploy --config deploy/ecspresso.yml
